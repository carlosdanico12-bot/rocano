// Por ahora, no se necesita JavaScript específico para esta página.
// Las validaciones y el envío se manejan del lado del servidor (PHP).
// Este archivo está aquí para futuras mejoras, como un selector de color.

document.addEventListener('DOMContentLoaded', function() {
    console.log('Página de configuración cargada.');
});
